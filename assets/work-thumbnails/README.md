# Work thumbnails

Upload new project thumbnail images to this folder.

Use relative paths in `portfolio.json`, for example:

```json
"image": "./assets/work-thumbnails/project-name.png"
```

After uploading an image, add the project title, link, description, tags, and image path to the `projects` array in `portfolio.json`.
